const defaultState = {
    num: 20
}

let reducer = (state = defaultState) => {
    let
    switch (key) {
        case value:
            
            break;
    
        default:
            break;
    }
}