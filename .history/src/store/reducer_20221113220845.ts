const defaultState = {
    num: 20
}

let reducer = (state = defaultState) => {
    let newS
    switch (key) {
        case value:
            
            break;
    
        default:
            break;
    }
}