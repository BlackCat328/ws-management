const defaultState = {
    num: 20
}

let reducer = (state = defaultState) => {
    let newState = JSON.p
    switch (key) {
        case value:
            
            break;
    
        default:
            break;
    }
}