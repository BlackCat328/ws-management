const defaultState = {
    num: 20
}

let reducer = (state = defaultState) => {
    let newState = J
    switch (key) {
        case value:
            
            break;
    
        default:
            break;
    }
}