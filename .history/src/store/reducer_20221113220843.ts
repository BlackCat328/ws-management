const defaultState = {
    num: 20
}

let reducer = (state = defaultState) => {
    let new
    switch (key) {
        case value:
            
            break;
    
        default:
            break;
    }
}