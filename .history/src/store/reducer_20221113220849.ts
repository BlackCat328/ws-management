const defaultState = {
    num: 20
}

let reducer = (state = defaultState) => {
    let newState = JSON
    switch (key) {
        case value:
            
            break;
    
        default:
            break;
    }
}